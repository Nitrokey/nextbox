#!/bin/bash -e

#echo "${TIMEZONE_DEFAULT}" > "${ROOTFS_DIR}/etc/timezone"
#rm "${ROOTFS_DIR}/etc/localtime"

#mkdir -p "${ROOTFS_DIR}/srv/nextcloud"
#mkdir -p "${ROOTFS_DIR}/srv/mariadb"


on_chroot << EOF



EOF
